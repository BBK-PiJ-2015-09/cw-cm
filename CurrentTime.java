import java.util.Calendar;

/**
 * A class to return the current time.
 */
public interface CurrentTime {

	/**
	 * Return the current time.
	 *
	 * @return a calendar set to the current time
	 */
	Calendar now();
}
